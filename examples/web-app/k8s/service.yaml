apiVersion: v1
kind: Service
metadata:
  name: k8s-web-app-service
  namespace: production
  labels:
    app: k8s-web-app
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: k8s-web-app

